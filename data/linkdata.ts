import { FC, ReactNode } from "react";
export type link={
  link:string;
  title:string;
  target:string;
  icon:ReactNode;
}

export type Headlink={
  link:string;
  title:string;
  detail:string;
  icon:ReactNode;
}
